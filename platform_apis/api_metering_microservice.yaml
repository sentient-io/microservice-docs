openapi: 3.0.1
info:
  title: API metering by microservice
  description: This microservice is used to view the API metering  graph for microservice.
  version: v0.1.0
  x-subscribelink: api_metering_microservice
servers:
  - url: 'https://dev-microserviceapi-catalog.sentient.io' 
tags:
- name: "v0.1.0"
  description: "Published Date - Jul 09, 2021"
paths:
  /apimeteringbymicroservice:
    post:
      tags:
        - v0.1.0
      responses:
        '200':
          description: success response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/success'
              example:
                activity: 'YES'
                message: Successfully Processed
                performance_per_microservice:
                  - api_result:
                      - message: ''
                        unsuccesscount: 1
                    total_unsuccess_call: 1
                status: Success
                usage_per_microservice:
                  - - '2021-01-27'
                    - 2
                    - 0
                  - - '2021-01-28'
                    - 2
                    - 0
                  - - '2021-02-02'
                    - 4
                    - 0
        '415':
          description: Invalid Input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/invalidinput'
        '500':
          description: Internal Server error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/servererror'
        
      security:
        - APIAuthorizer: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/input'
        required: true
components:
  schemas:
    success:
      type: object
      properties:
        activity:
          type: string
          example: YES
          description: Activity details
        performance_per_microservice:
          type: array
          example: [{<JSON result Object}]
          description: The list of array Json result object
          items:
            type: object
            properties:                      
                api_result:
                  type: array
                  description: The list of Json result object
                  items:
                    type: object
                    properties:
                      message:
                        type: string
                        description: Stauts of the api result
                        example: ""
                      unsuccesscount:
                        type: string
                        description: Unsuccess count of the api
                        example: 1
                total_unsuccess_call:
                  type: string
                  example: 1
                  description: Total unsuccess count of the api
        status:
          type: string
          example: Success
          description: The status of the API call success/failure
        usage_per_microservice:
          type: array
          example: ["2021-01-27",2,0]
          description: The list of Json result object
          items:
            type: object
    invalidinput:
      type: object
      properties:
        message:
          type: string
          example: Invalid Input
        status:
          type: string
          example: Failure
    servererror:
      type: object
      properties:
        message:
          type: string
          example: Internal Server error
        status:
          type: string
          example: Failure
    versions:
      type: object
      properties: 
        v0.1.0:
          type: array
          items:
            oneOf:
              - type: string
                example: Published Date - Jul 09, 2021
                description: Initial version release    
    input:
      type: object
      required:
        - from_date
        - to_date
      properties:
        from_date:
          x-name: from_date
          type: string
          default: True
          example: "2021-01-01"
          description: Api metering graph details from date in the format YYYY-DD-MM
        to_date:
          x-name: to_date
          type: string
          default: True
          example: "2021-02-24"
          description: Api metering graph details to date in the format YYYY-DD-MM
        org_id:
          x-name: org_id
          type: integer
          example: 1
          description: Org id to view the api metering graph details
        user_id:
          x-name: user_id
          type: integer
          example: 2
          description: User id to view the api metering graph details
        function_code:
          x-name: function_code
          type: string
          example: WIKIPEDIA
          description: Microservice function code to view the api metering graph details
  securitySchemes:
    APIAuthorizer:
      type: apiKey
      name: x-api-key
      in: header
      x-amazon-apigateway-authtype: custom
      description: Authentication is done using the x-api-key in the request header
