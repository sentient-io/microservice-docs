openapi: 3.0.1
info:
  title: Automatic Speech Recognition CHI (SG)
  description: |+
    This Voice microservice takes in the input audio file (in WAV format at 16kHz) and transcribes Mandarin speech content within the input file into text.

    This microservice works best when the audio file consists of mostly speech with no long pauses in between.
  x-additionalinfo: |+
    Suggested Use Cases:
    - Extraction of text from voice ( from sound files), especially with Singaporean accented Mandarin.

    - Extraction of text from video, especially with Singaporean accented Mandarin. 

    - Improve customer service - empower a customer service system by adding IVR (interactive voice response) and agent conversations to call centers. Perform analytics on conversation data to gain more insights into the calls and customers.

    - Enable voice control services - e.g. implement voice commands such as “turn the volume up,” and voice search such as saying “what is the temperature in Paris?”

    - Transcribe multimedia content - transcribe audio and video to include captions and improve audience reach and experience. For example, this microservice can be used for indexing or subtitling video and/or multispeaker content; similar to video captioning on YouTube.
  version: '0.1.1'
  x-subscribelink: automatic_speech_recognition_chi_sg
servers:
  - url: 'https://apis.sentient.io'
tags:
- name: "v0.1.1"
  description: "Published Date - Sep 28, 2018"
paths:
  /microservices/voice/asrsch/v0.1/getpredictions:
    post:
      tags:
        - v0.1.1
      responses:
        '200':
          description: success response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/success'
              example:
                message: "各位同学朋友老师教授大家好我叫墓园"
                status: success 
        '401':
          description: Unauthorized
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/unauthorized'
        '402':
          description: Payment Required
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/paymentrequired'
        '403':
          description: Forbidden
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/forbidden'
        '404':
          description: Not Found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/notfound'
        '419':
          description: Input Field Missing
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/inputfieldmissing'
        '420':
          description: Input Limit Exceeded
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/inputlimitexceeded'
        '422':
          description: Invalid Input
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/invalidinput'
        '500':
          description: Internal Server Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/servererror'
        '504':
          description: Gateway Timeout
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/gatewaytimeout'
      security:
        - APIAuthorizer: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/input'
        required: true
components:
  schemas:
    success:
      type: object
      properties:
              message:
                type: string
                example: 各位同学朋友老师教授大家好我叫墓园
                description: Converted Text
              status:
                type: string
                example: success
                description: The status of the API call success/failure
    unauthorized:
      type: object
      properties:
        message:
          type: string
          example: Missing Authentication Token
        status:
          type: string
          example: Failure
    forbidden:
      type: object
      properties:
        message:
          type: string
          example: Access Denied Unauthorized User
        status:
          type: string
          example: Failure
    paymentrequired:
      type: object
      properties:
        message:
          type: string
          example: Insufficient Credits Kindly Top Up
        status:
          type: string
          example: Failure
    notfound:
      type: object
      properties:
        message:
          type: string
          example: Invalid Request URL
        status:
          type: string
          example: Failure
    servererror:
      type: object
      properties:
        message:
          type: string
          example: Internal Server Error
        status:
          type: string
          example: Failure
    gatewaytimeout:
      type: object
      properties:
        message:
          type: string
          example: Request Timeout
        status:
          type: string
          example: Failure
    inputfieldmissing:
      type: object
      properties:
        message:
          type: string
          example: Input Field Missing
        status:
          type: string
          example: Failure
    inputlimitexceeded:
      type: object
      properties:
        message:
          type: string
          example: Input Limit Exceeded
        status:
          type: string
          example: Failure
    invalidinput:
      type: object
      properties:
        message:
          type: string
          example: Invalid Input
        status:
          type: string
          example: Failure
    input:
      type: object
      required:
        - audio 
      properties:
        audio:
          x-name: audio
          type: string
          default: true
          example: data:audio/wav;base64,UklGRkbiBABXQVZFZm10IBAAAAABA...
          description: |+
            Input .wav file in base64 format. 
            
            Input file needs to be prefixed by the data type and encoding.
            
            Data:audio/wav;base64,<wav in base64 string>
        language:
          x-name: language
          type: string
          default: true
          example: 1
          description: |+
            Language setting&#58;
              1 Mandarin
    versions:
      type: object
      properties: 
        v0.1.1:
         type: array
         items:
          oneOf:
            - type: string
              example: Published Date - Sep 28,2018
              description: Patch update on bug fixes
        v0.1.0:
         type: array
         items:
          oneOf:
            - type: string
              example: Published Date - Jun 21,2018
              description: Initial version release
  securitySchemes:
    APIAuthorizer:
      type: apiKey
      name: x-api-key
      in: header
      x-amazon-apigateway-authtype: custom
      description: Authentication is done using the x-api-key in the request header
